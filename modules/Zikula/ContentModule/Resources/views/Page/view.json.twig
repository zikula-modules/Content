{# purpose of this template: pages view json view #}
{% trans_default_domain 'page' %}
[
{% for page in items %}
    {% if not loop.first %},{% endif %}
    {{ page.toArray()|json_encode()|raw }}
{% endfor %}
]
