<p class="alert alert-info">{{ __('Select a geographical position either by clicking on the map or writing the position (latitude and longitude) directly. You can drag the map with your mouse, and double-click to zoom and select position.') }}</p>

<div class="content-openstreetmap">
    <div id="openstreetmap" class="map"></div>
</div>

{{ form_row(form.latitude) }}
{{ form_row(form.longitude) }}
{{ form_row(form.zoom) }}
{{ form_row(form.height) }}
{{ form_row(form.text) }}
